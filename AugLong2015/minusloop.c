#include <stdio.h>
int main()
{
    int num,i;
    scanf("%d", &num);
    for(i=(num-1); i >= 0 ;i--)
            {
                printf("i=%d\n",i);
                //count+=freq[i];
            }
}
